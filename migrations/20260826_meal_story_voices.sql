-- 「この肉の物語」公開ページ（追加のみ）
-- ラベルの数字キー(scan_code)から、その肉になった個体の一生を安全な範囲で見せ、
-- 食べた人が感想を残せるようにする。トレーサビリティの線の出口をつくる。
--
-- 公開範囲の方針: 捕獲者名・正確な座標・顧客情報は返さない。
--   返すのは 商品名/重さ/識別コード、獲れた場所(市＋地区)・日・方法・鳥獣種・性別・体重、
--   個体番号、放射能検査の結果、その個体から採れた部位、寄せられた声。
--
-- 適用済みの内容は本ファイルと同じ（apply_migration で適用）。
-- meal_voices: RLSで直接の読み書きは全拒否。下の2つのRPC経由でのみ触れる。
-- story_get(text)                          … 公開ページの表示データ
-- story_add_voice(text,text,int,text,text) … 感想の登録（1分に3件までの歯止め付き）
